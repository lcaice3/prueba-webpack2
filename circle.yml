machine:
  node:
    version: 6.11.4
  #ruby:
   # version: 2.4.1
angular-cli:
   version:1.3.0
dependencies:
  pre:
   - npm install -g @angular/cli
   - npm install -g gulp
  # - npm install webpack -g
  # - npm install aws-sdk
  # - npm i -S webpack-s3-uploader
   - npm install
   #- ng build -qa -aot
   
    
test:
  override:
    #- npm test
deployment:
  production:
    branch: master
    commands:
      - ng build qa -aot 
      #- NODENV=qa gulp build
      - gulp deploy
